// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /api/gdp/customerPickUpRate/getItemsByPaged': (req: Request, res: Response) => {
    res.status(200).send({});
  },
};
