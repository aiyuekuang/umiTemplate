// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /api/common/dropList/getCooperationStatus': (req: Request, res: Response) => {
    res.status(200).send({});
  },
};
