// @ts-ignore
import { Request, Response } from 'express';

export default {
  'POST /api/gdp/customerPickUpRate/getChartData': (req: Request, res: Response) => {
    res.status(200).send({});
  },
};
